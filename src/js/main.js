import Calculator from './calculator';

Calculator.start();

/**
 * I did use a second file to abstract all the Calculator logic away, so we can have a more clean organization,
 * it also makes the project ready to scale up later on if needed, to implement more features for example,
 * or to add more elements on the page that might need JavaScript logic.
 */
